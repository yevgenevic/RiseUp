# RiseUp: Fintech Banking Platform

Полнофункциональная платформа для управления филиалами, кредитных заявок, выявления мошенничества и финансовых рекомендаций с интеграцией ИИ через OpenRouter.

## 🎯 MVP Features

### 1. Справочник отделений + управление очередями
- Публичный каталог филиалов с часами работы и локацией
- Онлайн-бронирование номера в очередь
- Telegram-уведомления о приближении очереди
- Админка для управления расписанием филиалов
- Прогнозная нагрузка на филиалы

### 2. Интеллектуальный FAQ-чатбот
- Встроенный чат на сайте (React виджет)
- Telegram-бот с поддержкой команд
- RAG интеграция с базой документов
- Handover к живому оператору
- Кэширование ответов LLM

### 3. Финансовый ассистент
- Автоматическая категоризация расходов
- Прогнозирование доходов/расходов
- Персональные рекомендации
- Push и Telegram-уведомления о сбережениях
- Финансовые советы через LLM

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    Frontend (React/Vite)                │
│         Landing | Auth | Dashboard | Admin              │
└────────────────────┬────────────────────────────────────┘
                     │
┌────────────────────┴────────────────────────────────────┐
│              Backend API (Node.js/NestJS)               │
│  Auth | KYC | Accounts | Credit | Fraud | Admin | AI   │
└───┬────────────┬────────────┬──────────┬────────────────┘
    │            │            │          │
    ▼            ▼            ▼          ▼
┌─────────┐ ┌─────────┐ ┌───────────┐ ┌─────────────────┐
│PostgreSQL│ │  Redis  │ │ BullMQ    │ │ OpenRouter LLM  │
│Database  │ │ Cache   │ │(Job Queue)│ │ (via Proxy)     │
└─────────┘ └─────────┘ └───────────┘ └─────────────────┘
                │
    ┌───────────┴────────────┐
    ▼                        ▼
┌──────────────┐    ┌──────────────────┐
│Worker Service│    │Telegram Bot      │
│(Scoring,ML)  │    │Service           │
└──────────────┘    └──────────────────┘
```

## 📋 Требования

- Node.js 18+
- Docker & Docker Compose
- PostgreSQL 14+
- Redis 7+
- OpenRouter API ключ

## 🚀 Быстрый старт

### Локальная разработка (Docker Compose)

```bash
# Клонировать и перейти в проект
cd RiseUp

# Скопировать env файл
cp .env.example .env

# Запустить все сервисы
npm run dev

# Просмотр логов
npm run dev:logs

# Остановить
npm run dev:down
```

### Первая настройка БД

```bash
# Войти в контейнер PostgreSQL
docker exec -it fintech-postgres psql -U fintech_user -d fintech_db

# Или через Adminer (http://localhost:8080)
# Login: admin@admin.com | Password: admin
```

## 📦 Структура проекта

```
RiseUp/
├── backend/                 # Node.js API
│   ├── src/
│   │   ├── config/         # Конфиги (БД, JWT, etc)
│   │   ├── modules/        # Feature modules
│   │   ├── database/       # Миграции, seeders
│   │   ├── middleware/     # Auth, validation, error
│   │   └── utils/          # Helpers
│   └── package.json
├── frontend/               # React/Vite UI
│   ├── src/
│   │   ├── pages/         # Landing, Dashboard, Admin
│   │   ├── components/    # Reusable components
│   │   ├── hooks/         # Custom hooks
│   │   ├── styles/        # Global styles (Agrobank design)
│   │   └── api/           # API client
│   └── package.json
├── worker/                # Background jobs
│   ├── src/
│   │   ├── jobs/         # Job definitions
│   │   └── consumers/    # Job handlers
│   └── package.json
├── telegram-bot/          # Telegram bot service
│   └── src/
├── openrouter-proxy/      # LLM API wrapper
│   └── src/
├── docker/
│   ├── docker-compose.dev.yml
│   ├── docker-compose.prod.yml
│   ├── Dockerfile.backend
│   ├── Dockerfile.frontend
│   └── nginx.conf
├── scripts/              # Init, seed scripts
└── README.md
```

## 🔑 Ключевые API эндпоинты

### Аутентификация
- `POST /api/auth/register`
- `POST /api/auth/login`
- `POST /api/auth/logout`

### KYC
- `POST /api/kyc/upload`
- `GET /api/kyc/status`

### Счета и транзакции
- `GET /api/accounts`
- `GET /api/accounts/:id/transactions`
- `POST /api/accounts/:id/transfer`

### Кредиты
- `POST /api/credit/apply`
- `GET /api/credit/:id`

### Ветки и очереди
- `GET /api/branches`
- `POST /api/branches/:id/queue`
- `GET /api/branches/:id/queue/status`

### Чатбот (AI)
- `POST /api/chat/message`
- `GET /api/chat/history`

### Админка
- `GET /api/admin/users`
- `GET /api/admin/fraud/alerts`
- `GET /api/admin/credit/applications`

## 🎨 Дизайн

Цветовая схема вдохновлена Agrobank Uzbekistan:
- **Основной цвет:** Светло-зеленый (#66BB6A, #81C784)
- **Вторичный:** Белый (#FFFFFF)
- **Текст:** Темно-серый (#212121)
- **Акценты:** Зеленый (#43A047)

## 🔐 Безопасность

- JWT с refresh tokens
- 2FA (SMS, Telegram, Authenticator)
- Bcrypt для паролей
- PII шифрование (pgcrypto)
- Rate limiting и WAF
- Audit logs для всех критичных операций
- SAST/DAST при CI/CD

## 📊 Мониторинг

- **Prometheus:** метрики (latency, errors, queue depth)
- **Grafana:** дашборды
- **ELK/Graylog:** централизованное логирование
- **Sentry:** отслеживание ошибок
- **OpenTelemetry:** distributed tracing

## 🧪 Тестирование

```bash
# Unit тесты
npm run test

# Integration тесты
npm run test:integration

# E2E тесты
npm run test:e2e

# Coverage
npm run coverage
```

## 📝 Переменные окружения

Полный список в `.env.example`. Критичные:

| Переменная | Описание | Пример |
|-----------|---------|--------|
| `POSTGRES_PASSWORD` | БД пароль | `secure_password` |
| `JWT_SECRET` | JWT signing key | длинная случайная строка |
| `OPENROUTER_API_KEY` | LLM API ключ | `sk-or-v1-...` |
| `TELEGRAM_BOT_TOKEN` | Токен бота | от BotFather |

## 🚢 Деплой

### Docker Compose (dev/staging)

```bash
docker-compose -f docker/docker-compose.dev.yml up -d
```

### Production (Kubernetes)

```bash
kubectl apply -f k8s/
```

## 📞 Support

При вопросах - обратитесь к документации или откройте issue в GitHub.

## 📄 Лицензия

MIT
#   R i s e U p  
 